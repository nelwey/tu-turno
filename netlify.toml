# TanStack Start on Netlify — https://docs.netlify.com/build/frameworks/framework-setup-guides/tanstack-start/
[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  # Vite 8 expects Node 20.19+ or 22.12+; Netlify will use a current 22.x line.
  NODE_VERSION = "22"
